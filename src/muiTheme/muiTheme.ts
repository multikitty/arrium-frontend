import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    primary: {
      main: "hsl(220, 88%, 57%)",
      dark: "hsl(220, 88%, 47%)",
      light: "hsl(220, 88%, 67%)",
    },
  },
})

export default theme
