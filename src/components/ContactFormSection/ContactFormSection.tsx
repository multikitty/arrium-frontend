import React from "react"
import { StyledContactFormSection } from "./ContactFormSection.styled"

const ContactFormSection = () => {
  return <StyledContactFormSection></StyledContactFormSection>
}

export default ContactFormSection
