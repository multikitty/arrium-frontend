export enum UserRoles {
  driver = "driver",
  admin = "admin",
}

export enum DriverPages {
  blockAvailability = "blockAvailability",
  subscription = "subscription",
  faq = "faq",
  support = "support",
}
